# This file is intentionally left empty.
# Django uses DJANGO_SETTINGS_MODULE to determine which settings module to load.
# Do NOT import settings here — it causes double-loading and bypasses DJANGO_SETTINGS_MODULE.
